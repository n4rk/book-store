{# templates/base.html.twig #}
<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8"/>
		<title>
			{% block title %}📚 BookStore{% endblock %}
		</title>
		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css">
        <link rel="stylesheet" href="/css/style.css">
        {% block stylesheets %}{% endblock %}
	</head>
	<body>
        <nav class="navbar navbar-expand-lg navbar-light bg-light p-3">
            <div class="container">
                <div class="logo">
                    <a class="navbar-brand fw-bold" href="{{ path('livre_index') }}"><i class="bi bi-shop fs-4 me-2"></i> BookStore</a>
                </div>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="links" id="navbarSupportedContent">
                    <ul class="navbar-nav me-0 mb-2 mb-lg-0">
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('livre_index') }}">Livres</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('auteur_index') }}">Auteurs</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('genre_index') }}">Genres</a>
                        </li>
                        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                            <li class="nav-item ms-4">
                                <a class="nav-link text-danger fw-bold" href="{{ path('app_logout') }}">Se Déconnecter</a>
                            </li>
                        {% else %}
                            <li class="nav-item ms-4">
                                <a class="nav-link text-primary fw-bold" href="{{ path('app_login') }}">
                                    <i class="bi bi-box-arrow-in-right me-2"></i>Se Connecter
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link"></a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link text-success fw-bold" href="{{ path('app_register') }}">
                                    <i class="bi bi-plus-square me-2"></i>S'inscrire
                                </a>
                            </li>
                        {% endif %}
                    </ul>
                </div>
            </div>
        </nav>
		<div class="container my-5">
            {% block body %}{% endblock %}{% block javascripts %}{% endblock %}
		</div>

        <footer class="page-footer bg-dark font-small text-white">
            <div class="footer-copyright text-center py-3">
                Made by : <a href="https://github.com/n4rk" class="text-info text-decoration-none"> Hachmi Mohamed Amine</a> | © 2022
            </div>
        </footer>
	</body>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.10.2/dist/umd/popper.min.js" integrity="sha384-7+zCNj/IqJ95wo16oMtfsKbZ9ccEh31eOz1HGyDuCQ6wgnyJNSYdrPa03rtR1zdB" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.min.js" integrity="sha384-QJHtvGhmr9XOIpI6YVutG+2QOK9T+ZnN4kzFN1RtK3zEFEIsxhlmWl5/YESvpZ13" crossorigin="anonymous"></script>
</html>